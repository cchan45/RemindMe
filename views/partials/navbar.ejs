<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/reminders">Reminder List</a>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav mr-auto">
            <% if (user) { %>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                        Reminders
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="/reminders">List</a>
                        <a class="dropdown-item" href="/reminder/new">Create</a>
                    </div>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="/profile">Profile</a>
                </li>

            <% } %>
        </ul>

        <div class="my-2 my-lg-0 navbar-nav">
            <% if (user) { %>

                <% if (user.admin) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">Admin</a>
                    </li>
                <% } %>

                <li class="nav-item">
                    <a href="/auth/logout" class="btn btn-secondary">Logout</a>
                </li>

            <% } else { %>

                <li class="nav-item">
                    <a href="/auth/register" class="btn btn-danger">Register</a>
                </li>

            <% } %>
        </div>
    </div>

</nav>